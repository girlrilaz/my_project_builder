# Here you can define all your data sets by using simple YAML syntax.

# ---------------------------------------------------------
# Local dataset
# ---------------------------------------------------------

base:
  dir_path: ".."
  data_path:
    input: "data/raw"
    output: "data/processed"
    logs: "logs"
  data:
    rc: "rc.csv"
    v1: "v1.csv"
    v2: "v1.csv"

